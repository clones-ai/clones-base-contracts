{
  "name": "forge-data-marketplace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npx hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run --network baseSepolia deploy/01_deploy_factory.ts",
    "launch": "hardhat run --network baseSepolia deploy/02_launch_dataset.ts",
    "graduate": "hardhat run --network baseSepolia deploy/03_graduate.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomiclabs/hardhat-etherscan": "^3.0.0",
    "dotenv": "^16.6.1",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "ipfs-http-client": "^60.0.1"
  }
}
